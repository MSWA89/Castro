Put two files here, then set withAudio: true in src/Root.tsx:

  music.mp3        — the dark, elegant bed (see ../SCRIPT_VO.md)
  voiceover.mp3    — the FR voiceover (see ../SCRIPT_VO.md)

Until then, the opener renders silently with no errors.
